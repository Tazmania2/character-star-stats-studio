# Environment Variables Example
# Copy this file to .env.local for local development (optional)

# API Configuration (Optional - can be set at runtime via authentication form)
# VITE_API_URL=https://service2.funifier.com

# Note: Bearer tokens should NEVER be stored in environment variables
# They are entered at runtime through the authentication form
# and stored securely in the browser's localStorage
